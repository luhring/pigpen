{
  "name": "pigpen",
  "version": "0.1.0",
  "description": "a desktop utility for working with encrypted files",
  "main": "dist/main.js",
  "scripts": {
    "test": "exit 0",
    "build": "tsc",
    "app": "npm run build && electron ."
  },
  "author": "Dan Luhring",
  "license": "ISC",
  "dependencies": {
    "electron": "^1.7.5"
  },
  "devDependencies": {
    "@types/node": "^8.0.20",
    "typescript": "^2.4.2"
  }
}
